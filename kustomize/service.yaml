apiVersion: v1
kind: Service
metadata:
  name: api
  annotations:
    cloud.google.com/backend-config: '{"default": "api-backendconfig"}'
spec:
  selector:
    app: fastify-benchmark
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
